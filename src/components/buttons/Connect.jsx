import React from "react";

const Connect = ({ styles }) => (
  <button className={`py-2 px-8 font-poppins font-normal text-[18px] text-white gradient-bg-home border border-green-500 rounded-full outline-none ${styles}`}>
    G-WaterLink
  </button>
);

export default Connect;